/**
 * THE theme selector — the buyer customization surface, with your theme
 * package under src/themes/<name>/. Engine updates never touch this file.
 *
 * Point activeTheme at a directory name under src/themes/. Everything —
 * colors, fonts, layout, page composition, the 3D hero palette, OG images,
 * and the Studio cockpit — follows from that one line. Restart dev servers
 * after switching.
 */
export const activeTheme = "midnight";
